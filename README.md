## initial git project

```bash
# init project
git init

## setup project git
git config --global user.email <email-apa>
git config --global user.name <username-github>

## add remote repository
git add remote <your-remote-name> <your-repository-url>

## push to repository
git push -u <your-remote-name> <your-branch-name>
```